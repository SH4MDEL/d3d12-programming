빌보드
카메라가 이동하면? 특히 나무 뒤로 이동하면?
사각형이 항상 카메라를 향하도록 처리함.

빌보드의 중점 (Px, Py, Pz)
카메라의 위치 (Cx, Cy, Cz)일 때
Look = normalize(C - P)
Up = (0, 1, 0)
Right - Look * Up

빌보드와 플레이어의 고저차가 심하면 왜곡되어 보인다.


스카이박스
법선벡터는 안쪽으로
크기가 클 필요 없다.

D3D12_DEPTH_MASK_ZERO: depth buffer에 갱신하지 마라.

overdraw 문제
스카이박스도 깊이 검사를 한다. 단 셰이더에서 xyzw를 xyww로 선언하여 깊이를 항상 1로 설정.
오직 depth가 작을때만 그리는게 아니라 작거나 같을 때 그리게 하도록
depthStencilDesc.depthFunc을 D3D12_COMPARISON_FUNC_LESS_EQUAL로 선언


렌더 타겟 -> 뷰만 만듬
depth buffer -> create comitted resource로 사용.

PS가 단지 float를 리턴하는 것이 아니라 struct를 리턴하게 하고
구조체 내의 변수에 대해 SV_TARGET1..2..3을 정해준다.

하나의 리소스는 한 번에 하나의 서술자만 가질 수 있다.
어떤 상황에서는 리소스가 렌더 타겟으로 쓰이지만, 다른 순간에는 셰이더 리소스로 쓰인다?


멀티 렌더 타겟 뷰
맨 처음에는 셰이더가 SV_TARGET1..2..3 등등을 이용하여 여러 렌더타겟(리소스)에 그리고
그 리소스를 셰이더 리소스로 사용. 최종적으로 하나의 SV_TARGET으로 하나의 렌더타겟에 출력


바운더리? 윤곽
전면과 은면이 만나는 선에서 바운더리가 그려짐.
그러나 픽셀 셰이더에서 현재 렌더링하는 픽셀의 인접 픽셀의 색상을 알 수 없다.

포스트 프로세싱
셰이더를 통해 후처리